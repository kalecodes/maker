import React from 'react';

const Footer = () => {
    return (
        <footer>
            <div>
                &copy;{new Date.getFullYear()} by kalecodes
            </div>
        </footer>
    );
};

export default Footer;